spring:
    application:
        name: realtime-chatapp-persistence-service
    config:
        import: "configserver:"
    cloud:
        config:
            discovery:
                enabled: true
                service-id: realtime-chatapp-config-service

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/
